---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfje/java-buildpack

inputs:
- name: java-buildpack
- name: package-built

outputs:
- name: package-tagged

run:
  path: java-buildpack/ci/checksum-package.sh
